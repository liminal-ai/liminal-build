# Onboarding Docs

This directory is the stable home for repository onboarding and current-state
baseline artifacts.

When the current-state documentation process is re-run, refresh the files in
this directory in place rather than creating a new output location.
