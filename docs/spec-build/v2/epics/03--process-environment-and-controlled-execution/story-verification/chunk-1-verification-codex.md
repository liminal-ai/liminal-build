> Historical verification artifact. Superseded as current-state guidance by
> `implementation-addendum.md`. Retained for audit trail only.

VERDICT: BLOCK

| Item | Status | Evidence | Notes |
|---|---|---|---|
| 4. Artifact content persistence via Convex File Storage | REGRESSED | `convex/artifacts.ts:18-25` adds required `contentStorageId`; `convex/artifacts.ts:83-117` stores `Blob` via `ctx.storage.store()` and then calls the internal mutation; `convex/artifacts.ts:140-189` writes the storage id; `apps/platform/server/services/projects/platform-store.ts:904-917` switches the server path to `client.action()`; `apps/platform/server/services/processes/environment/process-environment.service.ts:567-571` is the production checkpoint call site; `apps/platform/server/services/projects/platform-store.ts:713-715` constructs `ConvexHttpClient` with no admin auth; `node_modules/.pnpm/convex@1.35.1/node_modules/convex/dist/cjs/browser/http_client.js:122-127` says admin auth is required to call internal queries/mutations/actions; `convex/_generated/ai/guidelines.md:81-82` says internal functions are private and callable only by other Convex functions. | The gap is not safely closed on the production path. The fake/unit path proves storage works, but the live server path now targets an `internalAction` over `ConvexHttpClient` without `setAdminAuth()`, so `ConvexPlatformStore.persistCheckpointArtifacts()` is not credibly callable in production. I also reproduced an orphan leak with a one-off fake-context run: calling the action with a missing process throws `Process not found.` and still leaves `storageCount 1`, because `convex/artifacts.ts:92-116` has no rollback if `ctx.storage.store()` succeeds before `ctx.runMutation()` fails. The delete helper exists at `convex/artifacts.ts:196-210`, and overwrite cleanup exists at `convex/artifacts.ts:255-257`, but no production code path currently calls the delete helper. Tests in `convex/artifacts.test.ts:120-221` are meaningful for file-storage mechanics, but they do not exercise the real `ConvexPlatformStore` path or any failure cleanup. |
| 5. `accessMode` added to `convex/sourceAttachments.ts` | SATISFIED | `convex/sourceAttachments.ts:5-24` adds durable required `accessMode`; `convex/sourceAttachments.ts:26-60` projects it; `apps/platform/server/services/projects/platform-store.ts:286-290` and `apps/platform/server/services/projects/platform-store.ts:803-807` wire the Convex query into the server store; `apps/platform/server/services/processes/readers/materials-section.reader.ts:76-95` carries `accessMode` into `currentSources`; `apps/platform/server/services/processes/environment/process-environment.service.ts:537-565` and `apps/platform/server/services/processes/environment/process-environment.service.ts:988-1000` feed source access modes into checkpoint planning; `apps/platform/server/services/processes/environment/checkpoint-planner.ts:16-25` enforces writability. | This is exercised on the production path both in bootstrap reads and in checkpoint planning. The Convex-layer test `convex/sourceAttachments.test.ts:102-182` would fail if the field disappeared from the durable projection. The server bootstrap tests `tests/service/server/process-work-surface-api.test.ts:1138-1262` are also meaningful and would fail if `accessMode` stopped flowing through materials. |
| 6. `workingSetFingerprint` compute + write + read-time stale comparison | WEAKENED | Spec target: `docs/spec-build/v2/epics/03--process-environment-and-controlled-execution/tech-design.md:312-329`. Implementation: `convex/processEnvironmentStates.ts:188-228` computes SHA-256 via Web Crypto over stable JSON; `convex/processEnvironmentStates.ts:234-292` reads canonical artifacts, outputs, sources, and provider kind; `convex/processEnvironmentStates.ts:378-389` does the read-time `ready -> stale` projection; `convex/processEnvironmentStates.ts:455-464` and `convex/processEnvironmentStates.ts:551-559` write fingerprints after env-state mutations; the production read path is `apps/platform/server/services/processes/readers/environment-section.reader.ts:7-10` -> `apps/platform/server/services/processes/process-work-surface.service.ts:453-466`; production write paths include `apps/platform/server/services/processes/process-start.service.ts:39-68`, `apps/platform/server/services/processes/process-resume.service.ts:42-69`, and multiple environment-service upserts such as `apps/platform/server/services/processes/environment/process-environment.service.ts:79-86`, `221-228`, `298-305`, `332-339`, `399-406`, `442-449`, `581-590`, `660-669`, `722-730`, `757-766`, `833-840`. | Most of the requirement is correctly implemented and the tests are strong: `convex/processEnvironmentStates.test.ts:385-525` verifies deterministic sorting, SHA-256 hex output, write-time storage, and read-time stale projection. The miss is that not every env-state write path stores a fingerprint: `convex/processes.ts:263-276` still inserts the initial `processEnvironmentStates` row from `createProcess` with `workingSetFingerprint: null`, and I reproduced that directly (`{\"state\":\"absent\",\"workingSetFingerprint\":null}`). Because the addendum explicitly required fingerprint storage on every env-state write path, this is partial rather than complete closure. |
| 9. `hasCanonicalRecoveryMaterials` alignment between stores | SATISFIED | `apps/platform/server/services/projects/platform-store.ts:940-950` makes `ConvexPlatformStore` include outputs in the OR; `apps/platform/server/services/projects/platform-store.ts:1596-1603` gives `InMemoryPlatformStore` the same logic and removes the old true-fallback; the production rebuild path consumes it at `apps/platform/server/services/processes/environment/process-environment.service.ts:124-133`. | This is now semantically aligned across stores. The new parity test `tests/service/server/platform-store-recovery-materials.test.ts:4-86` is non-tautological and would fail if the old in-memory fallback returned `true` on empty state or if outputs stopped counting. The previously permissive rebuild tests were also corrected to seed real materials instead of relying on the old fallback: `tests/service/server/process-actions-api.test.ts:1354-1415` and `tests/service/server/process-live-updates.test.ts:1315-1375`. |
| 11. Typed Convex compliance on `convex/sourceAttachments.ts` | SATISFIED | `convex/sourceAttachments.ts:1-3` now imports `QueryCtx`, `query`, `Doc`, and `Id`; `convex/sourceAttachments.ts:26-30` declares validators and a typed handler context; `convex/sourceAttachments.ts:31-60` no longer contains `queryGeneric`, `ctx: any`, or `any`-typed rows. | I verified there are no remaining `queryGeneric`, `ctx: any`, or `any` matches in this file. The query is fully validator-backed and typed. `corepack pnpm exec tsc --noEmit -p convex/tsconfig.json` also exits `0`, which is the right gate for this item. |
| 13. `process.hasEnvironment` derived from `processEnvironmentStates` | SATISFIED | Spec target: `docs/spec-build/v2/epics/03--process-environment-and-controlled-execution/tech-design-server.md:404-420`. Implementation: `convex/processEnvironmentStates.ts:72-85` encodes the true/false state rule, `convex/processEnvironmentStates.ts:341-349` patches the process row, `convex/processEnvironmentStates.ts:466` does that in the same mutation as env-state upsert, and `convex/processEnvironmentStates.ts:542-549` keeps the flag aligned when the hydration-plan mutation touches the env row. The derived field is consumed by raw process readers such as `convex/processes.ts:41-77`. | The production path does exercise this: start/resume/rehydrate/rebuild/execution/checkpoint all go through `upsertProcessEnvironmentState()`, so project-shell and raw process consumers now stay aligned instead of depending on process-surface compensation. The tests at `convex/processEnvironmentStates.test.ts:528-575` are meaningful and prove both the `true` transition (`preparing`) and the `false` transition (`lost`). |

## Gate Results

- `corepack pnpm run verify`: exit code `0`
- Convex tests: `29` passed
- Server tests: `111` passed
- Client tests: `152` passed
- Integration tests: `9` passed
- `corepack pnpm exec tsc --noEmit -p convex/tsconfig.json`: exit code `0`
- No failing test names to report

Note: the repo's `verify` script does not include integration tests, so the integration count above comes from a separate `corepack pnpm run test:integration` run.

## New Defects Found

- `ConvexPlatformStore.persistCheckpointArtifacts()` now routes through an `internalAction`, but the server never sets Convex admin auth. Evidence: `apps/platform/server/services/projects/platform-store.ts:713-715`, `apps/platform/server/services/projects/platform-store.ts:904-917`, and `node_modules/.pnpm/convex@1.35.1/node_modules/convex/dist/cjs/browser/http_client.js:122-127`. This makes the live server path for Item 4 non-credible even though the fake/test path passes.
- The new artifact action leaks stored blobs when any later step fails. Evidence: `convex/artifacts.ts:92-116` performs uploads with no compensating cleanup, and a one-off reproduction against `createFakeConvexContext()` left `storageCount 1` after the internal mutation threw `Process not found.`. Multi-artifact batches will leak every blob stored before the first failure.
